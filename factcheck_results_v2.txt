CLEAN: AabbExtensions
SKIP: AlwaysUpdatePhysicsWorld
ISSUES: ArchetypeChunk_DidChange
* The diagram incorrectly shows `archetype->Chunks.GetChangeVersion(...)` for retrieving the change version. Change versions are accessed via methods on the `ArchetypeChunk` struct itself (e.g., `chunk.GetChangeVersion(typeIndexInArchetype)`) or internal utility functions that operate directly on the chunk's data, not through an `archetype.Chunks` property.
* The `chunk.m_Chunk.ListIndex` parameter shown in the `GetChangeVersion` call (step 2) is not a standard or correct argument for retrieving a component's change version. The `ArchetypeChunk` instance already refers to the specific chunk.

SKIP: ArchetypeChunk_GetDynamicBufferAccessor
SKIP: BitArrayUtilities
ISSUES: BlobBuilderExtensions
- The diagram states that `BlobBuilderExtensions` exposes internal `BlobBuilder` mechanics via "unsafe reflection (BlobBuilderInternal)". Unity's `BlobBuilder` is designed for direct low-level memory manipulation using `unsafe` code and its public API, not through reflection on internal types like `BlobBuilderInternal` for core allocation and pointer patching.
- The descriptions for `Allocate(ref BlobBuilder, int size)` and `Allocate<T>(ref BlobBuilder, ref BlobPtr<T>, int size)` incorrectly state that raw `byte*` allocation and typed pointer allocation/patch registration occur "via BlobBuilderInternal". These operations are handled by `BlobBuilder`'s own internal mechanisms and public/unsafe methods.
- The description for `Allocate<T>(ref BlobBuilder, ref BlobPtr<T>, int size)` lists "patch registration" as an explicit functional output. Pointer patching is an internal process of `BlobBuilder` that resolves `BlobPtr<T>` offsets to actual memory locations during finalization, not an explicit return value or outcome of an `Allocate` method.

CLEAN: BlobCurve
ISSUES: BlobHashMapData
- The `BlobAssetHeader` in a `BlobAssetReference` does not contain an `Allocator` field. Blob assets are immutable unmanaged data, and the `Allocator` is specified during creation, not stored within the unmanaged header.

CLEAN: BlobPerfectHashMap
ISSUES: BurstTrampoline
- The `BURST COMPILED JOB` block incorrectly depicts `trampoline.Invoke(ref args)`. Burst-compiled code operates in an unmanaged context and cannot directly call a managed method with managed `ref` arguments. It would instead invoke an unmanaged function pointer with raw pointers and sizes.

ISSUES: BurstUtil
* `BurstUtil.IsEmpty` implies a direct call to `EntityQuery.IsEmpty`. However, `EntityQuery.IsEmpty` is a managed method and is not directly Burst-compatible. A method marked `[BurstCompile]` cannot simply call a non-Burst-compatible managed method.
* The `[BurstDiscard]` attribute on `SetNotBurstCompiled` means that any calls to this method from Burst-compiled code are entirely removed. Therefore, if a Burst-compiled job passes a `ref bool isBurstCompiled`, the variable in the Burst context will not be modified.
* The overview description of `BurstUtil` as a "thin `[BurstCompile]` wrapper around `EntityQuery.IsEmpty`" is inaccurate. `BurstUtil.IsEmpty` would need to re-implement the emptiness check using Burst-compatible operations.

ISSUES: CodecService
- The `Compress Flow` diagram states `boundedSize = GetBoundedSize(LZ4, srcSize)` which then calls `LZ4_compressBound(srcSize)`. The native `LZ4_compressBound` function only takes `srcSize` and does not accept an `LZ4` object or context parameter.
- The allocation method `dst = Memory.Unmanaged.Alloc` is not a standard Unity DOTS API. Typical allocations use `UnsafeUtility.Malloc` or `NativeArray` constructors with an `Allocator`.

CLEAN: CalculateEventMapBucketsJob
ISSUES: CodecService
*   The `CodecService.Compress()` and `CodecService.GetBoundedSize()` methods are depicted as taking an explicit `LZ4` parameter. For a static `CodecService` dedicated to wrapping `liblz4` via P/Invoke, this parameter is redundant and unidiomatic. The service itself embodies the LZ4 algorithm, and its methods would typically operate directly on the data without needing to be passed an `LZ4` identifier.

CLEAN: ComponentLookup_GetOptionalComponentDataRW
CLEAN: ComponentLookup_SetChangeFilter
CLEAN: ConvexHullBuilder
CLEAN: CurveRemapUtility
ISSUES: DebugUtil.SplitInt
1.  **Inconsistent "decimal parts" for negative numbers**: The algorithm's calculation of `decimals` in Step 3, using `math.frac(value)`, yields an unexpected value for negative inputs. Specifically, for `value = -10.1234` and `digits = 2`, `decimals` becomes `87`. This contradicts the typical interpretation of "decimal parts" (e.g., `12` for `0.12`) implied by the positive example `Result: integer=10, decimals=12 → "10.12"`, which suggests `decimals` represents the positive magnitude of the fractional digits for display purposes.

CLEAN: Deserializer
CLEAN: DistanceHitSortAscending
ISSUES: DynamicHashSet
- The memory layout diagram is incomplete and potentially misleading regarding the `Buckets` array. While `BucketCapacity` is stated as 16 in the header, the diagram visually represents `Keys` and `Next` arrays as having 8 elements (matching `Capacity`), but completely omits the `Buckets` array itself, which should logically contain 16 integer elements to support the `BucketCapacity`. This omission makes the overall memory structure unclear and inconsistent.

SKIP: DynamicMultiHashMap
ISSUES: DynamicUntypedBuffer
- The diagram labels the memory layout as "DynamicBuffer<byte> contents:". This incorrectly implies that the `DynamicBuffer<byte>` type itself natively implements or manages a complex internal structure consisting of a `DynamicUntypedBufferHelper` header and separate metadata arrays (Offsets, Sizes, Types, Alignments). In Unity ECS, `DynamicBuffer<byte>` is a low-level data structure designed solely to hold a contiguous array of `byte` elements. Any higher-level interpretation, such as a custom header or metadata arrays, must be implemented by a separate, custom data structure that *uses* the `DynamicBuffer<byte>` as raw memory, not by `DynamicBuffer<byte>` itself.
- The "Memory Layout" section provides explicit byte size calculations for `Offsets[Capacity]` (4 * Capacity bytes) and `Sizes[Capacity]` (4 * Capacity bytes), but it omits the corresponding byte sizes for `Types[Capacity]` and `Alignments[Capacity]`. This omission results in an incomplete and potentially misleading representation of the total memory consumption and layout, as `TypeHash` in Unity ECS is typically an 8-byte `ulong`.

CLEAN: DynamicVariableMap
CLEAN: ComponentLookup_GetOptionalComponentDataRW
CLEAN: ComponentLookup_SetChangeFilter
CLEAN: ConvexHullBuilder
CLEAN: CurveRemapUtility
ISSUES: DebugUtil.SplitInt
* The algorithm for `decimals` does not correctly handle negative input values. For `value = -10.1234` and `digits = 2`, `math.frac(value)` would return `-0.1234`, leading to `decimals = -12`. This contradicts the implied output format where `decimals` is presented as a positive magnitude.
* The claim of "enabling debug logging inside Burst-compiled jobs" is incomplete. Burst-compiled jobs have strict limitations on string manipulation.

ISSUES: Deserializer
- The `Deserializer` struct cannot be declared `unmanaged` if it contains a `NativeArray<byte>` field. `NativeArray<byte>` is a managed struct type due to its internal safety mechanisms and `IDisposable` interface.
- For `ReadBuffer<T>(n)` to guarantee "zero-copy reads" and "no allocations," it must return a `NativeSlice<T>` or a `T*`, not a new `NativeArray<T>`.

ISSUES: DistanceHitSortAscending
- The usage example converts a `NativeArray<DistanceHit>` to a managed array via `ToArray()` before sorting. In Unity ECS, `NativeArray<T>.Sort()` should be used directly.

ISSUES: DynamicHashSet
* The `DynamicHashMapHelper<int>` header is stated as 44 bytes, but based on the 10 listed `int` fields, its size should be 40 bytes (10 fields * 4 bytes/field).

ISSUES: DynamicMultiHashMap
* The diagram incorrectly labels the hash map as "open-addressed-with-chaining". The described implementation uses separate chaining, not open addressing.

ISSUES: DynamicUntypedBuffer
- The `DynamicUntypedBufferHelper` header is stated to be "40 bytes", but the sum of its 8 explicitly listed `int` fields is 32 bytes.

CLEAN: DynamicVariableMap
CLEAN: EnableMaskCreator
CLEAN: EntityLock
ISSUES: EnableMaskCreator
*   The `EnabledMask` constructor shown in the architecture diagram, `new EnabledMask(enableBitRef, ptrChunkDisabledCount)`, does not match the public constructors available for `Unity.Entities.EnabledMask` (Entities 1.x). Standard `EnabledMask` constructors typically require a `ref BlobAssetReference<EnabledState>` and an `int indexInChunk`, or a `ulong*` and `int*` for low-level internal construction.
*   The `SafeBitRef` struct, described as "A reference to a specific bit within a chunk's enable bit array" with both a `ptr: void*` and an `offset: int`, does not directly map to the `ulong* m_Bits` field expected by `Unity.Entities.EnabledMask`. `EnabledMask.m_Bits` represents the start of the entire bit array for a component type in a chunk, while `SafeBitRef` implies a specific bit within that array using an offset. Additional logic or a different interpretation of `SafeBitRef.ptr` would be required for direct compatibility, which is not indicated by the constructor signature.
*   The description for `int* ptrChunkDisabledCount` is truncated at the end (`*ptrChunkDisabledCoun`).

SKIP: EntityLock
ISSUES: EntityQueryBuilder_WithAllRW
*   The "WITH WithAllRW (clean)" example, `builder.WithAllRW(typeof(MyComponent))`, passes a `System.Type` to the method. However, the "IMPLEMENTATION" section's method signature, `WithAllRW(this EntityQueryBuilder builder, ComponentType type)`, expects a `ComponentType`. These are incompatible types.
*   If the method were intended to accept `System.Type` (as implied by the example usage), then `type.TypeIndex` within the implementation would be incorrect. A `System.Type` does not directly expose a `TypeIndex`; `TypeManager.GetTypeIndex(type)` would be required to get the TypeIndex.
*   The `ComponentType` constructor used in the "WITHOUT WithAllRW (verbose)" example, `ComponentType.ReadWrite<MyComponent>()`, is a static helper method, not a direct constructor as implied by the `new ComponentType { ... }` syntax in the implementation. While `new ComponentType { ... }` is technically valid if `ComponentType` has public setters, `ComponentType.ReadWrite<T>()` is the idiomatic way to construct it.

ISSUES: EntityQuery_GetFirstEntity
*   In Step 4, `ECS->GetArchetype(chunk)` is incorrect. There is no globally accessible `ECS` object used in this manner to retrieve an `Archetype` from a `Chunk`. The `Archetype` is typically accessed directly from the `Chunk` struct (e.g., `chunk.Archetype`) or via the `EntityManager`.

ISSUES: EntityQuery_GetSingletonBufferNoSync
- The diagram's "PURPOSE" section states that the extension "uses GetUnsafe on the BufferAccessor to bypass per-element safety checks." However, the code in "Step 3" (`ChunkIterationUtility.GetChunkBufferAccessor`) only shows retrieving a `BufferAccessor`. It does not illustrate any subsequent `GetUnsafe` call or mechanism to obtain a `DynamicBuffer<T>` that would bypass per-element safety checks. A `DynamicBuffer<T>` returned directly from such an accessor would typically still perform safety checks on element access.

ISSUES: EntityQuery_QueryHasSharedFilter
*   The generic constraint for `T` should not include `unmanaged`. `ISharedComponentData` types are managed types, and the `unmanaged` constraint is used for `IComponentData` or `IBufferElementData` that do not contain managed references.
*   An extension method in C# cannot directly access private/internal fields like `query.__impl->_QueryData->RequiredComponents`. This is an internal implementation detail of the `EntityQuery` class and would not be exposed this way to an extension.

ISSUES: EntityQuery_ReplaceSharedComponentFilter
*   The `where T : unmanaged, ISharedComponentData` constraint is incorrect. `ISharedComponentData` types are managed types, not `unmanaged`. The `unmanaged` constraint in C# applies to value types (structs) that do not contain any reference type fields.

SKIP: FixedArray
ISSUES: GlobalRandom
*   The `Random struct` is stated to be `20 bytes` with an internal structure of `[state: uint4 × 4 + index: int]`. This is incorrect for `Unity.Mathematics.Random`, which typically consists of two `uint` fields (e.g., `state` and `_data`), making it `8 bytes` in size.
*   The `Padding (44 bytes)` calculation is based on the incorrect `20 byte` size for the `Random struct`. If `Unity.Mathematics.Random` is `8 bytes`, then `56 bytes` of padding would be required to reach the `64-byte` cache line size.

SKIP: HSV
SKIP: HalfSizeTriangleMatrix
SKIP: IJobChunkWorkerBeginEnd
SKIP: IJobHashMapDefer
ISSUES: IJobParallelForDeferBatch
*   The `JobProducerType` attribute is applied directly to the `IJobParallelForDeferBatch` interface definition, not implicitly associated with the `IJobParallelForDeferBatchExtensions` class as suggested by its placement on the diagram.
*   The `IJobParallelForDeferBatch` interface is inherently designed for parallel execution. The inclusion of a `Schedule<T,U>(job, NativeList<U>, batch, dep)` method (without `Parallel` in its name) for this job type is unconventional and contradicts the explicit "ParallelFor" designation, as standard Unity Job System interfaces for parallel jobs typically only expose `ScheduleParallel` methods.

